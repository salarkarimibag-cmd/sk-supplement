// TODO: connect to MongoDB using the `MONGODB_URI` env var (see .env.local)
// once the `mongodb` (or `mongoose`) package is added to the project.
export async function connectToDatabase(): Promise<void> {
  throw new Error("connectToDatabase is not implemented yet.");
}
